#
# For new packages: First check if they're available via apt with apt search python3-<package>, and install those when available
# For packages not in apt: You can install them to your user directory with:
# pip3 install --user <package-name>
# apt = package mgr
#
# to run notebooks:
# sudo apt install jupyter-notebook
# sudo apt install python3-matplotlib
# sudo apt install python3-numpy
# pip3 install --user --break-system-packages paretoset pyarrow

# Python dependencies for polkadot-optimized build and benchmark suite
#
# =============================================================================
# RECOMMENDED: System Packages (Preferred Method)
# =============================================================================
#
# System packages are preferred because:
#   - Managed by package manager (automatic updates, clean uninstalls)
#   - Tested for compatibility with distro's Python version
#   - No conflicts between pip and system package manager
#   - Avoids "externally-managed-environment" errors on modern Python
#
# Arch/CachyOS:
#   sudo pacman -S python-psutil python-requests python-pandas python-pyarrow \
#                  python-tomlkit python-dateutil python-paretoset
#
# Ubuntu/Debian:
#   sudo apt install python3-psutil python3-requests python3-pandas python3-pyarrow \
#                    python3-tomlkit python3-dateutil
#   pip install --user paretoset  # Not available as system package
#
# =============================================================================
# ALTERNATIVE: pip install
# =============================================================================
#
# If you prefer pip (or system packages aren't available):
#
#   pip install --user -r requirements.txt              # user-local (recommended)
#   pip install --break-system-packages -r requirements.txt  # system-wide override
#   sudo pip install -r requirements.txt                # system-wide (not recommended)
#
# Version constraints ensure compatibility while allowing minor updates.
#
# =============================================================================

# Core dependencies for compile.py
tomlkit>=0.12.0         # TOML parsing for Cargo.toml manipulation
python-dateutil>=2.8.0  # Date/time calculations for build timing

# Dependencies for run_benchmarks.py
psutil>=5.9.0           # System and process utilities for benchmarking
requests>=2.31.0        # HTTP library for downloading chain specs

# Dependencies for parse_benchmarks.py
pandas>=2.0.0           # Data analysis and manipulation
pyarrow>=14.0.0         # Apache Arrow for efficient data storage (feather format)

# Dependencies for notebook analysis (optional but recommended)
paretoset>=1.2.0        # Pareto frontier analysis for optimization comparison

# =============================================================================
# Optional: Jupyter notebook support for analysis
# =============================================================================
# Uncomment if you want to use the notebooks in ./notebook/
#
# jupyter>=1.0.0
# matplotlib>=3.7.0
# seaborn>=0.12.0
